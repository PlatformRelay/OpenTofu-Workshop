# OpenTofu / Terraform working files
.terraform/
*.tfstate
*.tfstate.*
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Dependencies & build output
node_modules/
dist/
dist-*/
site/
*.local
stuff
.claude
.vscode
.envrc

# Slidev / Vite
.tmp-pages-nav/
.vite-inspect/
.remote-assets/
components.d.ts

# Export artifacts
*.pdf
.slides-export/
slidev-export/
slides-*-export/
slides-selection-export/
dist-selection/
.deck-selection.md

# OS & editors
.DS_Store
Thumbs.db
.idea/
*.swp
*.swo

# Logs & env
*.log
npm-debug.log*
.env
.env.*
!.env.example

# Untracked planning/working material — never published (ADR 0016).
agent-context/

# Local demo/reference clones (nested git repos) — facilitator rehearsal only.
references/

# Legacy sibling-worktree root — an unignored root lets `git add -A` stage another lane's tree (ADR 0016).
/.worktrees/

# verify.sh concurrency-guard lock + stale sidecars; root-anchored, trailing * load-bearing (ADR 0016).
/.verify.lock*

# OpenTofu working artifacts
.terraform/
*.tfstate
*.tfstate.*
crash.log
*.tfplan
# Provider lock files (.terraform.lock.hcl) are tracked — they pin versions
