# Learner-generated artifacts from running this lab in place — never committed.
# The lab's canonical HCL is main.tf (tracked, drift-checked). Everything below
# is generated at apply time, or is the scratch broken.tf from the break→fix.
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.*
*.tfstate
*.tfstate.*
build/
broken.tf
crash.log
crash.*.log
