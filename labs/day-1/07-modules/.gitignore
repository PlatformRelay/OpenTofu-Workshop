# Learner-generated artifacts from running this lab in place — never committed.
# The lab's canonical HCL is main.tf + modules/service-manifest/*.tf (tracked,
# drift-checked). Everything below is regenerated on init/apply.
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.*
*.tfstate
*.tfstate.*
out/
crash.log
crash.*.log
