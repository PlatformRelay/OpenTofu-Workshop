# Learner-generated artifacts from running this lab in place — never committed.
# The lab's canonical HCL is providers.tf + main.tf (tracked, drift-checked).
# Everything below is regenerated on init/apply.
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.*
*.tfstate
*.tfstate.*
crash.log
crash.*.log
