# OpenTofu SEC-4 offline pin fixture (US-P-PINS).
# Lines must match versions.env TOFU_VERSION. Live SHA256SUMS download+verify
# stays in setup/terratest/Dockerfile — this file is an offline name lock only.
ARTIFACT=tofu_1.10.3_linux_amd64.tar.gz
SUMS=tofu_1.10.3_SHA256SUMS
